// Database setup — creates pgvector schema
// Run: npm run setup
// Execution: Claude Code

console.log("Database setup — placeholder");
console.log("Will create:");
console.log("  - chats table (conversation history)");
console.log("  - knowledge_vectors table (pgvector embeddings)");
console.log("  - leads table (unhandled queries + OTP verification)");
console.log("  - audit_log table (query tracking)");
